database:
  driver: sqlite
  path: ./novel_together.db

app:
  host: "0.0.0.0"
  port: "3000"

jwt:
  secret: ""  # 必须填写

cors:
  origins: "*"  # 允许所有来源，或用逗号分隔如 "http://localhost:8081,http://localhost:3000"